# Direct dependencies only. Pinned so a rebuild six months from now produces
# the same image; transitive versions are resolved by pip.

# Web framework and server
fastapi==0.129.0
uvicorn[standard]==0.40.0

# Database: SQLAlchemy 2.x async, SQLite for local, Postgres for the cluster
SQLAlchemy==2.0.46
aiosqlite==0.22.1
asyncpg==0.30.0
# Sync driver, used only by Alembic (migrations are not async)
psycopg2-binary==2.9.10
alembic==1.18.4

# Config, validation, auth
pydantic==2.12.5
python-dotenv==1.2.1
PyJWT[crypto]==2.11.0

# Cluster state and inter-node calls
redis==6.4.0
httpx==0.28.1

# Function execution and the AI assistant
docker==7.1.0
groq==1.0.0
